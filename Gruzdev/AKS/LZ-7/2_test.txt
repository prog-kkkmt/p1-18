Выберите все подходящие ответы из списка
 
1) Тактовый генератор хранит текущее время и дату  - неверно

2) Некоторые компоненты компьютера меняют свое состояние в момент прихода импульса от тактового генератора  - верно, т.к. в момент прихода импульса от тактового генератора некоторые программы либо реализуют свои функции, либо же меняют своё состояние в момент прихода импульса.

3) Тактовый генератор задает общее время в системе - верно, т.к. Электронные компоненты входящие в состав вычислительной системы обычно взаимодействуют друг с другом, а значит они должны быть синхронизированы. А за синхронизацию как раз и отвечает тактовый генератор.

4) Скорость вычислений не зависит от частоты импульсов тактового генератора  - неверно