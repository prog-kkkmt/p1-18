Выполнил: Курбанмухаммедов С. Д, П1-18

	Тест.

Перечислите команды, корректно записанные с синтаксисом AT&T

push %ebx
mov $3, %eax
sub $3,%eax
mov %eax, $3
mov %eax %ebx

1. push %ebx
2. mov $3, %eax
3. sub $3, %eax