/ * * Матрикс 2. Даны целые положительные числа M и N. Сформировать целочисленную матрицу размера M ? N, у которой все элементы J-го столбца имеют значение 5·J (J = 1, …, N). */
// Попов А.А. 13.05.2020

#include <iostream>
использование  пространства  имен std;

int main(){
    size_t m, n;
 cout < < " Input M x N: ";
 cin > > > m > > > > > > > > n;
    int mtrx[m][n];
	для ( size_t i = 0; i){
		для ( size_t j = 0; j){
			int el = 5 * j;
 mtrx[i][j] = el;
		}
	}

	для ( size_t i = 0; i){
		для ( size_t j = 0; j)
 cout < < mtrx[i][j]<< '  ';
 cout <;
	}

	возврат  0;
}