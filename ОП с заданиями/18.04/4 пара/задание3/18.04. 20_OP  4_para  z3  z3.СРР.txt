/** Задача приветствует того, чьё имя вы введёте, только теперь чтение из файла и выводится в отдельный файл */

// Проверил исходник: Попов Антон, П1-18

# include < строка>
#include <fstream> 
использование  пространства  имен std;

int main(){
 строковое имя;
 ifstream input ("вход.txt");
 вход > > > имя;

 ofstream output ("выход.txt");
 выходные данные < < "Привет," < < имя <;

    возврат  0;
}