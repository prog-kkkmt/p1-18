/ * * Строка 29°. Дан символ C и строки S, S0. Перед каждым вхождением символа C в строку S вставить строку S0.*/

#include <iostream>
# include < строка>

использование  пространства  имен std;

int main(){

char c;
строка s, s0, s1;
unsigned short size_l;

 cout < < " введите свою строку: ";
	getline(cin, s);

 cout < < " введите строку для вставки:";
	getline(cin, s0);

 cout < < " Enter c: ";
 c = getchar();


 size_l = s. размер();
		для ( int i = 0; i){
			если (s[i] = = c)
 s1 += s0;
 s1 += s[i];
		}
	
 cout << s1 <;

    возврат  0;
}