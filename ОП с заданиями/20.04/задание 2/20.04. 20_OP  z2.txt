/**6. ”пражнение 3.7. Ќапишите программу, котора€ читает две строки и уведомл€ет, равны ли они, а если нет, кака€ из них больше.
»змените программу так, чтобы она указывала, имеют ли строки одинаковую длину, и если нет, то кака€ из них длинней.
*/
// ¬ыполнил: ѕопов јнтон, ѕ1-18

#include <iostream>
# include < строка>
использование  пространства  имен std;

int main(){
 строка s1, s2;
 cout < < " введите строку s1: ";
	getline (cin, s1);
 cout < < " введите строку s2: ";
	getline (cin, s2);
	если (s1 = = s2)
 cout < < "строки равны" <;
	остальное{
		если (s1 > s2)
 cout < < "s1 больше s2" <;
		еще 
 cout < < "s2 больше s1" <;
	}
	если (s1.size () = = s2.размер())
 cout < < "длины равны" <;
	остальное{
		если (s1.размер () > s2.размер())
 cout < < "s1 длиннее, чем s2" <;
		еще 
 cout < < "s2 длиннее, чем s1" <;
	}
   возврат  0;
}