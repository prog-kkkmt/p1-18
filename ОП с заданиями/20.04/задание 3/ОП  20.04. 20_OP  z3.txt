/**7. Упражнение 3.8. Напишите программу, способную читать строки со стандартного устройства ввода и соединять их в одну большую строку.
Отобразите полученную строку. Измените программу так, чтобы отделить соседние введенные строки пробелами.*/
// Выполнил: Попов Антон, П1-18

#include <iostream>
# include < строка>

использование  пространства  имен std;

int main(){
  int n;
 cout < < " введите свою цифру: ";
 cin > > > n;
 cout < < " введите 5 строк: ";
	getchar();
 строка s, s1, s2;
	для ( int i = 0; i){
		getline (cin, s);
 s1 += s;
}
 cout < < "ваша первая строка -" < < s1 <;
 cout < < " введите 5 строк: ";
	для ( int j = 0; j){
		getline (cin, s);
 s2 += s + " ";
	}
 cout < < ваша вторая строка - < < s2 <;
   возврат  0;
}