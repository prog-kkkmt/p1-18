/**
Текст44°. Дан текстовый файл, каждая строка которого изображает целое число, дополненное слева и справа несколькими пробелами. Вывести количество этих чисел и их сумму.
Выполнил: Попов Антон П1-18
*/

#include <iostream>
# include < строка>
#include <fstream>

использование  пространства  имен std;

int main(){
 ifstream file ("text.txt");
	
	int c;
	длинные n;
	чар знак;
	количество char;
	длинная сумма;
	
 n = 0; знак = 0;
 количество = 0; сумма = 0;

	while((c = file.get ())!= ВФ){
		if (c >= '0' && c <= '9') //Проверка на число
 n = n * 10 + c - '0';
		else if (c == '-') //Отрицательное число
 знак = 1;

		else if (c == '\n' && n == 0) //Стоит минус, а числа нет
 знак = 0;
		иначе  если (c = = '\n ' & & n != 0){ //Производим операции
 ++количество;
			если (знак = = 1)
 n * = - 1;
 сумма += n;
 n = 0;
 знак = 0;
		}
	}

 cout << "Колличество чисел: \n" << quantity 
 << "Сумма этих чисел: \n" << sum;
	
 файл.закрыть();

	возврат  0;
}