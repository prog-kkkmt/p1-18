/**Упражнение 3.14. Прочитайте некоторый текст, сохраняя каждое введенное слово как отдельный элемент вектора. 
Преобразуйте символы каждого слова в прописные. 
Отобразите преобразованные элементы вектора, выводя по восемь слов в строке.
*/
// Выполнил: Попов Антон, П1-18

#include <iostream>
# include < строка>
# включить < вектор>
использование std:: вектор;

int main() {
 std:: строка s1, s2;
 s2 = '0';
 вектор <std:: string> svec;
 std:: cout < < введите строки, пока не напишите 0:" <;
 std:: cin >>> s1;
    в то время как (s1 != s2){
	для (std:: string:: size_type i = 0; i != s1.size (); i++)
		если (islower (s1[i]))
 s1[i] = топпер (s1[i]);

 свек.push_back(s1);
 std:: cin >>> s1;
    }
 std:: cout <;
    для (вектор< int>:: size_type ix = > 0; ix != svec.размер (); ix++){
        если (ix % 8 = = 0)
 std:: cout <;
 std:: cout < < svec[ix]<<" ";
    }
 std:: cout <;
    возврат  0;
}