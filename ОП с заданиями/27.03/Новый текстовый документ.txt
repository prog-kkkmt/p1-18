/**Отрезок на плоскости задается координатами точек своих концов. Все координаты - целые числа, не превышающие 10^4.
Требуется вычислить длину этого отрезка с точностью до второго знака после запятой.*/

#include <stdio.h>
# include < математика.h>

typedef struct Coord{
	короткий  int x;
	короткий  int y;
} Точка;

typedef struct LineSegment{
	Пункт а;
	Пункт b;
	расстояние поплавка;
} Линия;

int main(){
 Линия lin;

	/ * Лин.a. x = 0;
 линь.a. y = 0;
 линь.b. x = 3;
 линь.b. y = 4;*/
	printf("Вводим a:\na.x = ");
	scanf ("%hd", &lin.a.x);
	printf ("a. y = ");
	scanf ("%hd", &lin.a.y);

	printf("\nВводим b:\nb.x = ");
	scanf ("%hd", &lin.b.x);
	printf ("b. y = ");
	scanf ("%hd", &lin.b.y);

 линь.расстояние = sqrtf (powf (lin.a.х-Лин.b.x, 2) + powf (lin.a.и-Лин.b.y, 2));

	printf ("lin: a (%d , %d ) b (%d , %d ), %0.2 f \n" , lin.a.х, Лин.a.й, Лин.b.х, Лин.b.й, Лин.расстояние);

	возврат  0;
}