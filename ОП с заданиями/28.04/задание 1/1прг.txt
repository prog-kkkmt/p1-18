/**Задание 1.
Реализовать на языке C++ программу для демонстрации работы функции бинарного поиска binSearch [1]*/

#include <iostream>
# включить < вектор>
#include <cmath>

использование  пространства  имен std;

typedef unsigned int Type;

int main(){
 вектор <Type > mas;
 Тип n;
 Тип цифра;
 Введите слева, справа, посередине;
 Тип неизвестен;

 cout << "Введите кол-во элементов в массиве: ";
 cin > > > n;
 cout << "Введите целочисленный массив: ";
	для (типа i = 0; i){
 cin > > > цифра;
 мас.push_back (цифра);
	}
	
 cout << "Введите искомое число: ";
 cin > > > неизвестно;
 слева = - 1;
 справа = n-1;
	
	в то время как (справа-слева > > 1){
	// / for (unsigned short i = 0; i){
 средний = (правый + левый) / 2;
		if (mas[middle] > = неизвестно)
 справа = посередине;
		еще
 левый = средний;
	}
	если (mas[справа] = = неизвестно)
 cout << "Ваше число попавсь. Его позиция в списке: " << right + 1 << endl;
	еще
 cout << "Ваше число не попавсь. Т.к его нет в данном списке" << endl;

	возврат  0;
}