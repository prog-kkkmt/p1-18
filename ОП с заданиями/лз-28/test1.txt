Выполнил: Попов Антон , П1-18

Тест 1.

1. Задать новое имя стандартному типу можно с помощью ключевого слова:

--> а) typedef; - True
 б) rename;
 в) в языке С++ задать новое имя стандартному типу нельзя.

2. Что представляет переменная str, введенная следующим образом:
 typedef char TEXT[20]
 Текст str[3];

--> а) массив из трех строк по 20 символов в каждой; - True
 б) массив символов из 60 элементов;
    в) в коде содержится ошибка, программа не скомпилируется.

3. Ключевое слово enum используется для:

-->    а) задания перечислений; - True
    б) определения структур с ограниченными возможностями;
    в) такое ключевое слово не используется в языке С++.

4. Структура — это:

 а) стандартный тип данных;
 б) тип данных, вводимый пользователем, позволяющий объединять
элементы одного типа;
--> в) тип данных, вводимый пользователем, позволяющий объединять
элементы разных типов. - Истинный

5. Выберите пример правильной инициализации структурной переменной
avto, содержащей сведения об автомобиле: марку, год выпуска, цвет:

 а) struct AVTO
        {
 модель char[20];
 int year;
 char color[20];
 } avto = {
 "Форд Фокус", 2006, " синий"
        };
 б) struct AVTO
        {
 модель char[20];
 int year;
 char color[20];
        };
 AVTO avto = {"Форд Фокус", 2006, " синий" };
--> в) оба варианта правильные. - Истинный

6. Укажите правильный вариант выделения памяти под структурную переменную:

--> а) anketa p; p = новая анкета; - True
 б) anketa *p; p = построить новую анкету;
 в) anketa *p; p = new anketa.

7. Выберите верное утверждение:

 а) объединение занимает меньше места в памяти по сравнению со
структурой с таким же набором полей;
 б) все поля объединения не могут использоваться одновременно; 
--> в) оба утверждения верны. - Истинный