/** 
Строка 58. Дана строка, содержащая полное имя файла, т. е. имя диска, список каталогов (путь), собственно имя и расширение. Выделить из этой строки имя файла (без расширения).
*/
# include < строка>
#include <iostream>

использование  пространства  имен std;

int main(){

строка s, s1, s2;
int n = 0;
 cout < < " введите полный путь к файлу: ";
	getline(cin, s);

		для ( int i = s. size ()- 1; i > = > 0; i--)
			если (s[i]=='.'){
 n = -- i;
				перерыв;}

		в то время как (s[n]!= '/'){
 s1 += s[n];
 --северный;
		}

		для ( int j = s1.size ()- 1; j > = > 0; j--)
 s2 += s1[j];

 cout < < "Назовите свой файл (без расширения) -" < < s2 <;
   возврат  0;
}

/**
Пример:
Ввод:
Введите полный путь к файлу: C://PutKFilu/Rom4ik/Ci_files/NewFile-да.формат txt
Выход:
Назовите свой файл (без расширения) - NewFile
*/