/ * * Строка 13. Дана строка. Подсчитать количество содержащихся в ней цифр.*/

#include <iostream>
# include < строка>

использование  пространства  имен std;

int main(){

unsigned  short count = 0, size_l;
строка s;

 cout < < " введите свою строку: ";
	getline(cin, s);
 size_l = s. размер();

	ибо (int i = 0; i != size_l; ++i)
		если (s[i] > = > ' 0 '& & s[i] < = ' 9 ')
 ++считать;

 cout < < "всего цифр в строке:" < < count <;

    возврат  0;
}