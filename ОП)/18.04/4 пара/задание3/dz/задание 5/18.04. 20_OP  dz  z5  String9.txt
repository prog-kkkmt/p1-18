/ * * String9°. Дано четное число N (> 0) и символы C1 и C2. Вывести строку длины N, которая состоит из чередующихся символов C1 и C2, начиная с C1. */

# include < строка>
#include <iostream>

использование  пространства  имен std;

int main(){

неподписанный  символ c1, c2;
int n;
строка s;

 cout < < " Enter digit: ";
 cin > > > n;

 cout < < " Enter char one: ";
 cin >>> c1;

 cout < < " Enter char two: ";
 cin >>> c2;

		для ( int i = 0; i)
			если (i % 2 = = 0)
 s += c1;
			еще 
 s += c2;
 cout < < ваша строка на выходе - <;
   возврат  0;
}