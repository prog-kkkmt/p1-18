3.2. Что такое стандартный конструктор?
Стандартный конструктор - Это конструктор у которого выполнение осуществляеться автоматическим, если осуществляющий  инициализацию объекта конструктор не вызван в коде явно. Также стандартный конструктор используеться независимо от того, где именно определена переменная.

3.3. Назовите три способа инициализации объекта класса string.
1 Инициализация строки как копия другой строки перемеенной
2 Инициализация строки как копии строкового литерала
3 Инициализация строки последовательностью из N символов C

3.4. Каковы значения переменных s и s2?

string s; 
int main() { 
	string s2;
}

Обе переменные это пустые строки так как использован стандартный конструктор
отличие в них в том что s глобальная переменная , а s2 локальная.

3.5. Напишите программу, читающую со стандартного устройства ввода целые строки. 
Измените программу так, чтобы она читала отдельные слова.


3.6. Объясните, как поступают с символами предваряющих пробелов при чтении данных в строковую переменную, 
когда используется функция get line ().
В отличие от оператора ввода, функция getline() не игнорирует предворяющие пробелы. Однако каждый раз, когда функция getline() встречает символ новой строки, даже если это первый введенный символ, она останавливает чтение из буфера ввода и завершает работу.

3.2.4
6. Упражнение 3.7. Напишите программу, которая читает две строки и уведомляет, равны ли они, а если нет, какая из них больше. 
Измените программу так, чтобы она указывала, имеют ли строки одинаковую длину, и если нет, то какая из них длинней.

3.8. Напишите программу, способную читать строки со стандартного устройства ввода и соединять их в одну большую строку. 
Отобразите полученную строку. Измените программу так, чтобы отделить соседние введенные строки пробелами.
3.9. Что делает следующая программа? Действительно ли она допустима? Если нет, то почему?
string s;
cout << s[0] << endl;
Данная программа выводит первый символ строки s. Она не допустима так как s не инициализирована значит s[0] находиться вне диапозона являеться серьезной ошибкой.

3.10. Напишите программу поиска в строке знаков пунктуации. 
Программа должна позволить ввести символьную строку, содержащую знаки пунктуации, и вывести ту же строку, но уже без знаков пунктуации.