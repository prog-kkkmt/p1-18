/** Выполнид Роголев В.А.
В программе объявлены две переменные целого типа. На каждую из них ссылается указатель. На первую ссылается указатель p1, а на вторую указатель p2. Используя указатели, вывести значения этих переменных(сначала вторую, потом первую), а затем их сумму. Числа должны быть разделены пробелом.(функцию main() писать не нужно)
*/

printf("%d %d %d",*p2,*p1 ,*p2+*p1);