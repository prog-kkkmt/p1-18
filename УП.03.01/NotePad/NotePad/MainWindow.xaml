<Window x:Class="NotePad.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NotePad"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
	<Grid>
		<DockPanel>
			<Menu DockPanel.Dock="Top"
			Background="White"
			Grid.Column="0" Grid.ColumnSpan="10" Grid.Row="0"
			VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
			VerticalContentAlignment="Bottom" HorizontalContentAlignment="Center"
			FontSize="15" FontFamily="Calibri"
			>
				<MenuItem Header="File" x:Name="FileMenuItem">
					<MenuItem x:Name="OpenMenuItem" Header="Open" Click="OpenMenuItem_Click"/>
					<MenuItem x:Name="SaveMenuItem" Header="Save"
						Click="SaveMenuItem_Click"/>
					<MenuItem x:Name="ExitMenuItem" Header="Exit" />
				</MenuItem>
			</Menu>
			
			<ScrollViewer
				x:Name="NotePadScrollViewer"
				Grid.Column="0" Grid.ColumnSpan="10" Grid.Row="1" Grid.RowSpan="9"
				BorderThickness="10">
				<TextBox x:Name="NotePadTextBox"
				    FontSize="20" FontFamily="Century Gothic"
				    BorderThickness="3"
				    TextWrapping="Wrap"
				    AcceptsReturn="True"
				    KeyDown="Window_KeyDown"/>
			</ScrollViewer>
		</DockPanel>
	</Grid>
</Window>
